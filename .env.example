# SEC asks automated clients to identify themselves. Use your real email in local .env.
SEC_USER_AGENT=SectorPulse your_email@example.com

# Optional: point the dashboard at a live warehouse instead of the committed sample DB.
SECTOR_PULSE_DB=data/sample.duckdb
